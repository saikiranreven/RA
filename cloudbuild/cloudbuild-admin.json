{
  "steps": [
    {
      "name": "gcr.io/cloud-builders/gcloud",
      "args": ["app", "deploy", "admin-app/app.yaml", "--quiet"]
    }
  ],
  "timeout": "600s",
  "options": {
    "logging": "CLOUD_LOGGING_ONLY"
  }
}